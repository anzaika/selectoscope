web:
  extends:
    file: common.yml
    service: web
  environment:
    - PASSENGER_APP_ENV=development
  volumes:
    - ".:/home/app/webapp"
    - "./config/sites_enabled:/etc/nginx/sites-enabled/"
  links:
    - redis

sidekiq:
  extends:
    file: common.yml
    service: sidekiq
  environment:
    - PASSENGER_APP_ENV=development
  volumes:
    - ".:/home/app/webapp"
  links:
    - redis

redis:
  extends:
    file: common.yml
    service: redis
